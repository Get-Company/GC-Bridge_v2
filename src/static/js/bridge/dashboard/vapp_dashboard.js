const app = Vue.createApp({
    delimiters: ['${', '}'],
    data() {
        return {
            orders: [],
            progress: -1, // Init value for the progress bar
        };
    },
    methods: {

    },

})